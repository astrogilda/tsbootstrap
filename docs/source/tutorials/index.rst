Tutorials
=========

Runnable notebooks that teach the library by example. Each one renders here with
its saved outputs and can be launched interactively on Colab or Binder using the
badges at the top of the notebook.

The suite is being built in waves. More notebooks (the per-method deep dives, the
"which bootstrap when" decision guide, and the uncertainty-quantification
showcase) are on the way.

.. nbgallery::
   :caption: Notebook gallery
   :name: notebook-gallery

   quickstart
